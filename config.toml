[server]
address = "0.0.0.0"
port = 8080
output_directory = "./downloads"
max_clients = 100
# Optional: Override auto-calculated values
# parallel_streams = 16  # Auto-calculated if not specified
# buffer_size = 16777216  # Auto-calculated based on file size
# socket_send_buffer_size = 16777216  # Auto-calculated based on file size
# socket_recv_buffer_size = 16777216  # Auto-calculated based on file size
enable_compression = false

[client]
server_address = "127.0.0.1"
server_port = 8080
# Optional: Override auto-calculated values
# parallel_streams = 16  # Auto-calculated based on file size if not specified
# buffer_size = 16777216  # Auto-calculated based on file size
# socket_send_buffer_size = 16777216  # Auto-calculated based on file size
# socket_recv_buffer_size = 16777216  # Auto-calculated based on file size
enable_compression = false

[security]
auth_token = "shift_default_token"
max_connections_per_ip = 10

[performance]
simd_enabled = true
zero_copy_enabled = true
memory_pool_size = 2000
connection_pool_size = 100
compression_level = 1
metrics_enabled = true
